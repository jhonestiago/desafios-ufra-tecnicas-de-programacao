# Task 16

## PT

Quais são os valores atribuídos a **z**, se adicionarmos as instruções:

1. z := x / y;
2. z := x DIV y;
3. z := x % y; (O símbolo % representa o resto da divisão)

```plaintext
ALGORITMO Exercicio
VAR
    INTEIRO x, y;
    REAL z;
INICIO
    x := 15;
    y := 2;
    //instrução 1 aqui
    //instrução 2 aqui
    //instrução 3 aqui
FIM
```

## EN

What are the values ​​assigned to **z**, if we add the following instructions:

1. z := x / y;
2. z := x DIV y;
3. z := x % y; (The % symbol represents the remainder of the division)

```plaintext
ALGORITHM Exercise
VAR
    INTEGER x, y;
    REAL z;
BEGIN
    x := 15;
    y := 2;
    //instruction 1 here
    //instruction 2 here
    //instruction 3 here
END
```
