base:float = float(input('Insira a medida da base do retângulo (em cm): '))
altura:float = float(input('Insira a medida da altura do retângulo (em cm): '))

area_retangulo = base * altura

print(f'A área do retângulo é: {area_retangulo:.2f} cm^2')
